# Expo HAS CHANGED

This app is on **Expo SDK 54** (downgraded from 57 on 2026-08-22, deliberately — see
`CLAUDE.md`'s mobile-app note — so Expo Go's App Store build can run it directly on a
physical iPhone without a custom dev client). Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code.
